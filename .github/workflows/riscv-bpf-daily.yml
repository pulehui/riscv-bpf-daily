# SPDX-License-Identifier: Apache-2.0
#
# RISC-V BPF daily test run.

name: riscv-bpf-daily

on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  issues: write

env:
  IMAGE: ghcr.io/${{ github.repository_owner }}/riscv-bpf-vmtest

jobs:
  vmtest-riscv64:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Sync Runner Image
        run: bash ./scripts/sync_image.sh

      - name: Restore ccache
        uses: actions/cache@v4
        with:
          path: ccache-dir
          key: ccache-riscv64-${{ github.run_id }}
          restore-keys: |
            ccache-riscv64-

      - name: Setup Tree
        id: setup
        run: bash ./scripts/run_ci.sh setup

      - name: Run test_verifier
        id: test_verifier
        if: always() && steps.setup.outcome == 'success'
        run: bash ./scripts/run_ci.sh test_verifier

      - name: Run test_progs
        id: test_progs
        if: always() && steps.setup.outcome == 'success'
        run: bash ./scripts/run_ci.sh test_progs

      - name: Archive Logs
        if: always() && (steps.setup.outcome == 'failure' || steps.test_verifier.outcome == 'failure' || steps.test_progs.outcome == 'failure')
        uses: actions/upload-artifact@v4
        with:
          name: bpf_vmtest-log
          path: |
            *.log
            *.stdout
            *_errors.txt
          if-no-files-found: warn
          retention-days: 30

      - name: File Issue
        if: always() && (steps.setup.outcome == 'failure' || steps.test_verifier.outcome == 'failure' || steps.test_progs.outcome == 'failure')
        uses: actions/github-script@v7
        env:
          BPF_COMMIT: ${{ steps.setup.outputs.bpf_commit }}
        with:
          script: |
            const fileIssue = require('./scripts/file_issue.js');
            await fileIssue({ github, context, core });
